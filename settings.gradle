// Configure Gradle build cache
buildCache {
    local {
        enabled = true
        directory = file("${rootDir}/.gradle/build-cache")
    }
}
// Add global settings for all subprojects
gradle.beforeProject {
    group = 'com.nuvei' // Set global group
    version = '1.0-SNAPSHOT' // Set global version
}
